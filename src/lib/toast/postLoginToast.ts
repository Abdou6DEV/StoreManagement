/** Layout kicks off daily backup this many ms after the main app mounts (see layout.tsx). */
export const POST_LOGIN_BACKUP_ENSURE_DELAY_MS = 3000;

/** Default auto-dismiss duration for non-sticky toasts (see toastContext). */
export const TOAST_AUTO_DISMISS_MS = 4000;
