import "./index.css";
import ReactDOM from "react-dom/client";

ReactDOM.createRoot(document.getElementById("root")).render(
  <h1 className="text-gray-200 bg-gray-800 font-bold rounded-3xl p-4 m-4">
    Hello from React + Vite + Tailwindcss!
  </h1>,
);
